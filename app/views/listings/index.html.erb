<h1>Listing Index</h1> 

<%= will_paginate @listings %>
<br>
<table>
</table>
<% @listings.each do |l| %>
	<li><td>
			<tr><%= l.title %></tr>
		<tr><%= link_to " Show ", listing_path(l.id) %></tr> 
		<tr><%= link_to " Edit ",
		edit_listing_path(l.id) %></tr>
		<tr><%= link_to " Book ", book_path(l.id), method: :post, data: {confirm: "Do you wish to proceed with your booking?"} %></tr>
		<tr><%= link_to " Delete ", listing_path(l.id), method: :delete, data: {confirm: "Are you sure?"} %></tr> </p>

		<p>Tags: 
		<% l.taggables.each do |t| %>
		<%= t.tag.name %>
		<% end %></p>

		<p>Created by:
		<% if l.user %>
			<tr><%= l.user.email %></tr>
		<% end %></p>
		<br>
	</td></li>
<% end %>
</ul>
</table>
<%= will_paginate @listings %>


<p><%= link_to "Create new listing", new_listing_path %></p>
<p><%= link_to "Back", root_path %></p>